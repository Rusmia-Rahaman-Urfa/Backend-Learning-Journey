// Create an array of 3 colors and print each color using a for loop.

const color = ["Red", "Orange", "Blue"];
let text = ""
for(let i = 0; i < color.length; i++){
    text += color[i];
}
 console.log(color);